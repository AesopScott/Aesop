name: AIP Auto-Patch courses.html

on:
  push:
    paths:
      - 'aip/drafts/*.json'
  workflow_dispatch:

concurrency:
  group: aip-autopatch
  cancel-in-progress: true

jobs:
  autopatch:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.AIP_COMMIT_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Run AIP auto-patch
        run: python .github/scripts/aip_autopatch.py

      - name: Rebuild draft index
        run: |
          python3 -c "
          import os, json
          d = 'aip/drafts'
          files = sorted(f for f in os.listdir(d) if f.endswith('.json') and f != 'index.json')
          with open(os.path.join(d, 'index.json'), 'w') as fp:
              json.dump(files, fp, indent=2)
          "

      - name: Commit patched courses.html
        run: |
          git config user.name "AIP Auto-Patch"
          git config user.email "aip-agent@aesopacademy.org"
          git add ai-academy/courses.html aip/drafts/index.json
          git diff --staged --quiet || git commit -m "AIP: auto-patch courses.html with approved drafts"
          git pull --rebase origin main
          git push
